#![feature(
    test,
    array_value_iter,
    try_blocks,
    box_syntax,
    core_intrinsics,
    partition_point,
    exact_size_is_empty,
    array_windows,
    array_chunks
)]

mod input;
mod utils;

pub mod solutions;
